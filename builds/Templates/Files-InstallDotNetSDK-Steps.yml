parameters:
  dotNetVersion: 7.0.x

steps:
  - task: UseDotNet@2
    displayName: 'Use .NET SDK v${{ parameters.dotNetVersion }}'
    inputs:
      packageType: sdk
      version: ${{ parameters.dotNetVersion }}
      includePreviewVersions: true
