<Page
    x:Class="Files.GenericFileBrowser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Files"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d" ScrollViewer.HorizontalScrollBarVisibility="Hidden" ScrollViewer.VerticalScrollBarVisibility="Auto">

    <Page.Resources>
        <AcrylicBrush x:Key="RegAcrylic" BackgroundSource="HostBackdrop" TintColor="White" TintOpacity="0.7" FallbackColor="{StaticResource SystemChromeMediumColor}"/>
        <AcrylicBrush x:Key="InAppAcrylic" BackgroundSource="Backdrop"  TintColor="#FF16B7E8" TintOpacity="0.6" FallbackColor="{StaticResource SystemChromeHighColor}"/>
        <AcrylicBrush x:Key="LightAcrylic" BackgroundSource="HostBackdrop" TintColor="White" TintOpacity="0.8" FallbackColor="{StaticResource SystemChromeMediumColor}"/>
        <SolidColorBrush x:Key="GreyFallback" Color="{StaticResource SystemChromeMediumColor}"/>
        <Storyboard x:Name="PointerHoverButton1">
            <FadeOutThemeAnimation Storyboard.TargetName="Shadow1"/>

        </Storyboard>
        <local:BackState x:Key="BackObject"/>
        <local:FowardState x:Key="FowardObject"/>

    </Page.Resources>
    <Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <!--VisualState to be triggered when window width is >=0 effective pixels.-->
                        <AdaptiveTrigger MinWindowHeight="0"/>
                    </VisualState.StateTriggers>

                    <VisualState.Setters>

                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowHeight="1080"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>

                    </VisualState.Setters>
                </VisualState>


            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>



        <Grid Tag="{x:Bind Tag, Mode=OneWay}" Background="White" Margin="0,0,0,0">
            <Grid Name="ProgressIndicator" Visibility="{x:Bind local:ItemViewModel.PVIS.isVisible, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Canvas.ZIndex="5" Background="White" Margin="0,255,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" Height="815" Width="1865">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressRing IsActive="True" Canvas.ZIndex="5" Width="100" Height="100" />
                    <TextBlock Text="{x:Bind local:ItemViewModel.PROGRESSUI.prog, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
            <Grid DataContext="{Binding BackObject}" HorizontalAlignment="Stretch" Height="250" VerticalAlignment="Top">
                <Button x:FieldModifier="public" IsEnabled="{x:Bind local:ItemViewModel.BS.isEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Click="Back_Click" x:Name="Back" Foreground="DarkGray" Content="&#xE76B;" FontFamily="Segoe MDL2 Assets" Background="#33FFFFFF" FontSize="20" Margin="40,52,0,0" VerticalAlignment="Top">

                </Button>
                <Grid DataContext="{Binding FowardObject}">
                    <Button IsEnabled="{x:Bind local:ItemViewModel.FS.isEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" x:Name="Foward" Foreground="DarkGray" Content="&#xE76C;" FontFamily="Segoe MDL2 Assets" Background="#33FFFFFF" FontSize="20" Margin="115,52,0,0" VerticalAlignment="Top"/>
                </Grid>
                <Button x:Name="Up" Foreground="DarkGray" Content="&#xE74a;" FontFamily="Segoe MDL2 Assets" Background="#33FFFFFF" FontSize="20" Margin="190,52,0,0" VerticalAlignment="Top"/>
                <ScrollViewer HorizontalScrollMode="Auto" HorizontalScrollBarVisibility="Auto" Margin="0,148,0,25" HorizontalAlignment="Stretch" VerticalAlignment="Center" >
                    <TextBlock x:Name="VisiblePath" Text="C:\" Margin="50,0,0,0" FontFamily="Segoe UI Black" FontWeight="Bold" FontSize="48" ScrollViewer.HorizontalScrollBarVisibility="Auto" VerticalAlignment="Top" HorizontalAlignment="Stretch"></TextBlock>
                </ScrollViewer>

            </Grid>

            <Grid Padding="50,250,50,0">


                <ListView x:Name="AllView" SelectionMode="Single" IsItemClickEnabled="False" SelectionChanged="AllView_ItemClick" ItemsSource="{x:Bind ViewModel.FilesAndFolders}" Margin="0,0,0,0">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:ListedItem">

                            <StackPanel Orientation="Horizontal" Spacing="50"  Width="1500" Height="45">
                                <Grid  Margin="15, 0, 0, 0">
                                    <FontIcon Visibility="{Binding FolderImg}" Glyph="&#xE8D5;" FontFamily="Segoe MDL2 Assets" Foreground="#ffe793"/>
                                    <Image Visibility="{Binding FileIconVis}" Height="20" Width="20" Source="{Binding FileImg}" Stretch="UniformToFill" />
                                </Grid>
                                <TextBlock Text="{Binding FileName}" Width="400" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Center"></TextBlock>
                                <TextBlock Text="{Binding FileDate}" Width="200"  HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Center"></TextBlock>
                                <TextBlock Text="{Binding FileExtension}" Width="200" HorizontalAlignment="Right" Margin="0,0,0,0" VerticalAlignment="Center"></TextBlock>
                            </StackPanel>

                        </DataTemplate>
                    </ListView.ItemTemplate>

                </ListView>



            </Grid>
        </Grid>





    </Grid>


</Page>