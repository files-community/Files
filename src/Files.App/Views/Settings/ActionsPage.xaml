<!--  Copyright (c) Files Community. Licensed under the MIT License.  -->
<Page
	x:Class="Files.App.Views.Settings.ActionsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:CommunityToolkit.WinUI.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:dataitems="using:Files.App.Data.Items"
	xmlns:helpers="using:Files.App.Helpers"
	xmlns:keyboard="using:Files.App.UserControls.KeyboardShortcut"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:uc="using:Files.App.UserControls"
	x:Name="PageRoot"
	DataContext="{x:Bind ViewModel, Mode=OneWay}"
	Loaded="ActionsPage_Loaded"
	mc:Ignorable="d">

	<Page.Resources>
		<ResourceDictionary>
			<ResourceDictionary.ThemeDictionaries>
				<ResourceDictionary x:Key="Light">
					<StaticResource x:Key="OutlinedStyleBackgroundBrush" ResourceKey="ControlFillColorTransparentBrush" />
				</ResourceDictionary>
				<ResourceDictionary x:Key="Dark">
					<StaticResource x:Key="OutlinedStyleBackgroundBrush" ResourceKey="ControlFillColorSecondaryBrush" />
				</ResourceDictionary>
				<ResourceDictionary x:Key="HighContrast">
					<StaticResource x:Key="OutlinedStyleBackgroundBrush" ResourceKey="ControlFillColorTransparentBrush" />
				</ResourceDictionary>
			</ResourceDictionary.ThemeDictionaries>

			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="ms-appx:///UserControls/KeyboardShortcut/KeyboardShortcut.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Page.Resources>

	<Grid>
		<StackPanel
			HorizontalAlignment="Stretch"
			VerticalAlignment="Stretch"
			Spacing="4">

			<!--  Title  -->
			<TextBlock
				Padding="0,0,0,12"
				Style="{StaticResource App.Theme.SubtitleTextBlockStyle}"
				Text="{helpers:ResourceString Name=Actions}" />

			<!--  Commands  -->
			<controls:SettingsExpander
				Margin="0,4,0,0"
				HorizontalAlignment="Stretch"
				Header="{helpers:ResourceString Name=Commands}"
				IsExpanded="True">
				<controls:SettingsExpander.HeaderIcon>
					<FontIcon Glyph="&#xE765;" />
				</controls:SettingsExpander.HeaderIcon>

				<!--  Add command; Restore defaults lives in the split flyout  -->
				<SplitButton
					x:Name="AddCommandSplitButton"
					Click="AddCommandButton_Click"
					Content="{helpers:ResourceString Name=AddCommand}">
					<SplitButton.Flyout>
						<MenuFlyout Placement="Bottom">
							<MenuFlyoutItem
								Command="{x:Bind ViewModel.ShowRestoreDefaultsConfirmationCommand, Mode=OneWay}"
								Text="{helpers:ResourceString Name=RestoreDefaults}">
								<MenuFlyoutItem.Icon>
									<FontIcon Glyph="&#xE777;" />
								</MenuFlyoutItem.Icon>
							</MenuFlyoutItem>
						</MenuFlyout>
					</SplitButton.Flyout>
				</SplitButton>

				<controls:SettingsExpander.ItemsHeader>
					<Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
						</Grid.RowDefinitions>

						<!--  List label + search  -->
						<Grid Padding="16,12" ColumnSpacing="12">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="Auto" />
							</Grid.ColumnDefinitions>

							<TextBlock
								VerticalAlignment="Center"
								FontWeight="SemiBold"
								Text="{helpers:ResourceString Name=KeyboardShortcuts}" />

							<TextBox
								x:Name="SearchBox"
								Grid.Column="1"
								Width="240"
								PlaceholderText="{helpers:ResourceString Name=Search}"
								TextChanged="SearchBox_TextChanged" />
						</Grid>

						<!--  Divider  -->
						<Border
							Grid.Row="1"
							Height="1"
							Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

						<!--  List Of Available Key Bindings (ItemsRepeater virtualizes against the page host scroll)  -->
						<ItemsRepeater
							x:Name="ValidKeyBindingsRepeater"
							Grid.Row="2"
							Margin="0,4"
							ItemsSource="{x:Bind ViewModel.FilteredActionItems, Mode=OneWay}">
							<ItemsRepeater.Layout>
								<StackLayout />
							</ItemsRepeater.Layout>
							<ItemsRepeater.ItemTemplate>
								<DataTemplate x:DataType="dataitems:ModifiableActionItem">
									<UserControl PointerEntered="RootGrid_PointerEntered" PointerExited="RootGrid_PointerExited">
										<!--  Transparent background so the whole row is hit-testable for hover  -->
										<Grid
											MinHeight="44"
											Padding="16,0"
											Background="Transparent"
											ColumnSpacing="12">
											<Grid.ColumnDefinitions>
												<ColumnDefinition Width="*" />
												<ColumnDefinition Width="Auto" />
												<!--  Reserve room for the row actions so revealing them shifts nothing  -->
												<ColumnDefinition Width="72" />
											</Grid.ColumnDefinitions>

											<!--  Command Description  -->
											<TextBlock
												Grid.Column="0"
												VerticalAlignment="Center"
												Text="{x:Bind CommandDescription}"
												TextTrimming="CharacterEllipsis" />

											<!--  Key Binding Control  -->
											<keyboard:KeyboardShortcut
												Grid.Column="1"
												VerticalAlignment="Center"
												HotKeys="{x:Bind KeyBindings, Mode=OneWay}" />

											<!--  Row Actions (revealed on pointer over)  -->
											<StackPanel
												x:Name="RowActions"
												Grid.Column="2"
												HorizontalAlignment="Right"
												Orientation="Horizontal"
												Spacing="4"
												Visibility="Collapsed">
												<!--  Edit Button  -->
												<Button
													Width="32"
													Height="32"
													Padding="0"
													AutomationProperties.Name="{helpers:ResourceString Name=Edit}"
													Click="EditButton_Click"
													Tag="{x:Bind}"
													ToolTipService.ToolTip="{helpers:ResourceString Name=Edit}">
													<FontIcon FontSize="14" Glyph="&#xE70F;" />
												</Button>

												<!--  Delete Button (opens a confirmation flyout)  -->
												<Button
													Width="32"
													Height="32"
													Padding="0"
													AutomationProperties.Name="{helpers:ResourceString Name=Delete}"
													Click="DeleteButton_Click"
													Tag="{x:Bind}"
													ToolTipService.ToolTip="{helpers:ResourceString Name=Delete}">
													<FontIcon FontSize="14" Glyph="&#xE74D;" />
												</Button>
											</StackPanel>
										</Grid>
									</UserControl>
								</DataTemplate>
							</ItemsRepeater.ItemTemplate>
						</ItemsRepeater>
					</Grid>
				</controls:SettingsExpander.ItemsHeader>
			</controls:SettingsExpander>

		</StackPanel>

		<!--  Restore Defaults Confirmation TeachingTip  -->
		<TeachingTip
			x:Name="RestoreDefaultsConfirmationTeachingTip"
			ActionButtonCommand="{x:Bind ViewModel.RestoreDefaultsCommand, Mode=OneWay}"
			ActionButtonContent="{helpers:ResourceString Name=Yes}"
			ActionButtonStyle="{ThemeResource AccentButtonStyle}"
			CloseButtonContent="{helpers:ResourceString Name=No}"
			IsLightDismissEnabled="True"
			IsOpen="{x:Bind ViewModel.IsResetAllConfirmationTeachingTipOpened, Mode=TwoWay}"
			PreferredPlacement="BottomLeft"
			Subtitle="{helpers:ResourceString Name=RestoreDefaultKeybindingsConfirmation}"
			Target="{x:Bind AddCommandSplitButton}" />

		<!--  Delete Key Binding Confirmation TeachingTip  -->
		<TeachingTip
			x:Name="DeleteConfirmationTeachingTip"
			ActionButtonClick="DeleteConfirmationTeachingTip_ActionButtonClick"
			ActionButtonContent="{helpers:ResourceString Name=Yes}"
			ActionButtonStyle="{ThemeResource AccentButtonStyle}"
			CloseButtonContent="{helpers:ResourceString Name=No}"
			IsLightDismissEnabled="True"
			PreferredPlacement="Left"
			Subtitle="{helpers:ResourceString Name=DeleteKeyBindingConfirmation}" />
	</Grid>
</Page>
