<vm:BasePropertiesPage
	x:Class="Files.App.Views.Properties.HashesPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:helpers="using:Files.App.Helpers"
	xmlns:local="using:Files.App.Views.Properties"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:models="using:Files.Backend.Models"
	xmlns:toolkitconverters="using:CommunityToolkit.WinUI.UI.Converters"
	xmlns:vm="using:Files.App.ViewModels.Properties"
	DataContext="{x:Bind HashesListView, Mode=OneWay}"
	mc:Ignorable="d">

	<vm:BasePropertiesPage.Resources>
		<ResourceDictionary>
			<toolkitconverters:BoolNegationConverter x:Key="BoolNegationConverter" />

			<toolkitconverters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
		</ResourceDictionary>
	</vm:BasePropertiesPage.Resources>

	<Grid x:Name="RootGrid">

		<Grid
			x:Name="HashesListGrid"
			Grid.Row="1"
			Margin="12"
			VerticalAlignment="Top"
			x:Load="{x:Bind HashesViewModel.CanAccessFile, Mode=OneWay}"
			Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
			BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
			BorderThickness="1"
			CornerRadius="4">
			<ListView
				x:Name="HashesListView"
				ItemsSource="{x:Bind HashesViewModel.Hashes, Mode=TwoWay}"
				SelectedItem="{x:Bind HashesViewModel.SelectedItem, Mode=TwoWay}">

				<!--  Header  -->
				<ListView.Header>
					<Grid
						Height="44"
						Padding="16,8,16,0"
						BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
						BorderThickness="0,0,0,1"
						ColumnSpacing="12">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="64" />
							<ColumnDefinition Width="1" />
							<ColumnDefinition Width="*" />
						</Grid.ColumnDefinitions>
						<TextBlock
							VerticalAlignment="Center"
							Style="{StaticResource BodyTextBlockStyle}"
							Text="Algorithm" />
						<Border
							Grid.Column="1"
							Width="1"
							Height="36"
							VerticalAlignment="Bottom"
							Background="{ThemeResource DividerStrokeColorDefaultBrush}" />
						<TextBlock
							Grid.Column="2"
							VerticalAlignment="Center"
							Style="{StaticResource BodyTextBlockStyle}"
							Text="Hash value" />
					</Grid>
				</ListView.Header>

				<!--  Template  -->
				<ListView.ItemTemplate>
					<DataTemplate x:DataType="models:HashInfoItem">
						<ListViewItem IsSelected="{x:Bind IsSelected, Mode=TwoWay}" ToolTipService.ToolTip="{x:Bind HashValue}">
							<Grid ColumnSpacing="12">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="64" />
									<ColumnDefinition Width="1" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>

								<!--  Algorithm Name  -->
								<TextBlock
									VerticalAlignment="Center"
									Style="{StaticResource BodyTextBlockStyle}"
									Text="{x:Bind Algorithm}" />

								<!--  Hash Value  -->
								<TextBlock
									Grid.Column="2"
									VerticalAlignment="Center"
									Style="{StaticResource BodyTextBlockStyle}"
									Text="{x:Bind HashValue}"
									TextTrimming="CharacterEllipsis"
									TextWrapping="NoWrap" />

								<!--  Copy button  -->
								<Button
									x:Name="CopyHashButton"
									Background="Transparent"
									BorderBrush="Transparent"
									Command="{Binding ViewModel.CopyHashValueCommand, ElementName=HashesListView, Mode=OneWay}"
									Visibility="{x:Bind IsSelected, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
									<FontIcon Glyph="&#xE8C8;" />
								</Button>
							</Grid>
						</ListViewItem>
					</DataTemplate>
				</ListView.ItemTemplate>

			</ListView>
		</Grid>

		<!--  Unavailable Message  -->
		<TextBlock
			x:Name="UnavailableMessageTextBlock"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			x:Load="{x:Bind HashesViewModel.CanAccessFile, Converter={StaticResource BoolNegationConverter}, Mode=OneWay}"
			Style="{StaticResource BodyStrongTextBlockStyle}"
			Text="{helpers:ResourceString Name=HashesUnavailable}" />

	</Grid>
</vm:BasePropertiesPage>
