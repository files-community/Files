<!--  Copyright (c) 2023 Files Community. Licensed under the MIT License. See the LICENSE.  -->
<local:StandardViewBase
	x:Class="Files.App.Views.LayoutModes.DetailsLayoutBrowser"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:behaviors="using:Files.App.Data.Behaviors"
	xmlns:converters="using:Files.App.Converters"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:filesystem="using:Files.App.Utils"
	xmlns:filetags="using:Files.Core.ViewModels.FileTags"
	xmlns:helpers="using:Files.App.Helpers"
	xmlns:i="using:Microsoft.Xaml.Interactivity"
	xmlns:icore="using:Microsoft.Xaml.Interactions.Core"
	xmlns:local="using:Files.App.Views.LayoutModes"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:toolkit="using:CommunityToolkit.WinUI.UI.Controls"
	xmlns:uc="using:Files.App.UserControls"
	xmlns:wct="using:CommunityToolkit.WinUI.UI"
	xmlns:wctconverters="using:CommunityToolkit.WinUI.UI.Converters"
	x:Name="PageRoot"
	DataContext="{x:Bind ViewModel, Mode=OneWay}"
	NavigationCacheMode="Enabled"
	mc:Ignorable="d">

	<local:StandardViewBase.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="ms-appx:///ResourceDictionaries/DefaultGridSplitterStyle.xaml" />
				<ResourceDictionary Source="ms-appx:///ResourceDictionaries/DetailsLayoutModeStyles.xaml" />
			</ResourceDictionary.MergedDictionaries>

			<converters:StringToBrushConverter x:Key="StringToBrushConverter" />
			<converters:BoolToSelectionModeConverter x:Key="BoolToSelectionModeConverter" />
			<wctconverters:BoolNegationConverter x:Key="BoolNegationConverter" />
			<wctconverters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
			<wctconverters:BoolToVisibilityConverter
				x:Key="NegatedBoolToVisibilityConverter"
				FalseValue="Visible"
				TrueValue="Collapsed" />
			<wctconverters:EmptyObjectToObjectConverter
				x:Key="EmptyObjectToObjectConverter"
				EmptyValue="Collapsed"
				NotEmptyValue="Visible" />

			<Style BasedOn="{StaticResource HeaderGridSplitterStyle}" TargetType="toolkit:GridSplitter" />

		</ResourceDictionary>
	</local:StandardViewBase.Resources>

	<i:Interaction.Behaviors>
		<icore:EventTriggerBehavior EventName="PointerWheelChanged">
			<icore:InvokeCommandAction Command="{x:Bind CommandsViewModel.PointerWheelChangedCommand}" />
		</icore:EventTriggerBehavior>
		<icore:EventTriggerBehavior EventName="PointerPressed">
			<icore:InvokeCommandAction Command="{x:Bind CommandsViewModel.ItemPointerPressedCommand}" />
		</icore:EventTriggerBehavior>
	</i:Interaction.Behaviors>

	<Grid
		x:Name="RootGrid"
		ContextFlyout="{x:Bind BaseContextMenuFlyout}"
		SizeChanged="RootGrid_SizeChanged">

		<!--  Empty Indicator  -->
		<uc:FolderEmptyIndicator
			Grid.Row="3"
			Margin="0,125,0,0"
			HorizontalAlignment="Center"
			VerticalAlignment="Top"
			Canvas.ZIndex="0"
			EmptyTextType="{x:Bind ParentShellPageInstance.FilesystemViewModel.EmptyTextType, Mode=OneWay}" />

		<!--  Invalid Item Name Tip  -->
		<TeachingTip
			x:Name="FileNameTeachingTip"
			CloseButtonContent="{helpers:ResourceString Name=OK}"
			PreferredPlacement="Auto"
			Subtitle="{helpers:ResourceString Name=FileNameTeachingTip/Subtitle}"
			Visibility="Collapsed" />

		<!--  Semantic Zooming  -->
		<SemanticZoom
			x:Name="RootGridZoom"
			CanChangeViews="{x:Bind CollectionViewSource.IsSourceGrouped, Mode=OneWay}"
			ViewChangeStarted="SemanticZoom_ViewChangeStarted"
			Visibility="{x:Bind FolderSettings.IsLayoutModeChanging, Mode=OneWay, Converter={StaticResource NegatedBoolToVisibilityConverter}}">

			<!--  Main View  -->
			<SemanticZoom.ZoomedInView>
				<ListView
					x:Name="FileList"
					Padding="8,0,8,12"
					HorizontalAlignment="Stretch"
					VerticalContentAlignment="Stretch"
					wct:ScrollViewerExtensions.EnableMiddleClickScrolling="{x:Bind IsMiddleClickToScrollEnabled, Mode=OneWay}"
					x:FieldModifier="public"
					AllowDrop="{x:Bind InstanceViewModel.IsPageTypeSearchResults, Converter={StaticResource BoolNegationConverter}, Mode=OneWay}"
					AutomationProperties.AccessibilityView="Raw"
					CanDragItems="True"
					ContainerContentChanging="FileList_ContainerContentChanging"
					DoubleTapped="FileList_DoubleTapped"
					DragItemsStarting="FileList_DragItemsStarting"
					DragOver="ItemsLayout_DragOver"
					Drop="ItemsLayout_Drop"
					FocusVisualPrimaryThickness="0"
					FocusVisualSecondaryThickness="0"
					IsDoubleTapEnabled="True"
					IsTabStop="True"
					ItemsSource="{x:Bind CollectionViewSource.View, Mode=OneWay}"
					Loaded="FileList_Loaded"
					PreviewKeyDown="FileList_PreviewKeyDown"
					ScrollViewer.HorizontalScrollBarVisibility="Auto"
					ScrollViewer.HorizontalScrollMode="Auto"
					SelectionChanged="FileList_SelectionChanged"
					SelectionMode="Extended"
					Tapped="FileList_ItemTapped"
					Visibility="{x:Bind FolderSettings.IsLayoutModeChanging, Converter={StaticResource NegatedBoolToVisibilityConverter}, Mode=OneWay}">

					<!--  ListView Item Transitions  -->
					<ListView.ItemContainerTransitions>
						<TransitionCollection>
							<AddDeleteThemeTransition />
						</TransitionCollection>
					</ListView.ItemContainerTransitions>

					<!--  ListView Header  -->
					<ListView.Header>
						<Grid
							x:Name="ListViewHeaderGrid"
							Height="40"
							BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
							BorderThickness="0,0,0,1"
							ContextFlyout="{x:Bind ViewModel.ColumnHeadersMenuFlyout, Mode=OneWay}"
							PointerPressed="Grid_PointerPressed" />
					</ListView.Header>

					<!--  Item Template  -->
					<ListView.ItemTemplate>
						<DataTemplate x:DataType="filesystem:ListedItem">
							<UserControl>
								<StackPanel
									HorizontalAlignment="Stretch"
									AutomationProperties.Name="{x:Bind Name, Mode=OneWay}"
									IsRightTapEnabled="True"
									Loaded="StackPanel_Loaded"
									Orientation="Horizontal">
									<!--  Item Thumbnail  -->
									<Grid
										Width="24"
										HorizontalAlignment="Left"
										VerticalAlignment="Stretch">

										<!--  Thumbnail  -->
										<Grid
											x:Name="IconBox"
											Width="20"
											Height="Auto"
											HorizontalAlignment="Center"
											VerticalAlignment="Center"
											AutomationProperties.Name="{helpers:ResourceString Name=FileBrowserThumbnailIconColumnGrid/AutomationProperties/Name}"
											Opacity="{x:Bind Opacity, Mode=OneWay}"
											TabFocusNavigation="Local"
											Tag="ItemImage">
											<ContentPresenter
												x:Name="PicturePresenter"
												Width="20"
												Height="20"
												HorizontalAlignment="Center"
												VerticalAlignment="Center"
												x:Load="{x:Bind LoadFileIcon, Mode=OneWay}"
												CornerRadius="{StaticResource DetailsLayoutThumbnailCornerRadius}">
												<Image
													x:Name="Picture"
													Source="{x:Bind FileImage, Mode=OneWay}"
													Stretch="Uniform" />
											</ContentPresenter>
											<Image
												x:Name="FolderGlyphIcon"
												Width="20"
												Height="20"
												HorizontalAlignment="Stretch"
												VerticalAlignment="Stretch"
												x:Load="{x:Bind LoadDefaultIcon, Mode=OneWay}"
												Source="{x:Bind PlaceholderDefaultIcon, Mode=OneWay}" />
											<Border
												x:Name="TypeUnknownGlyph"
												Width="20"
												Height="20"
												HorizontalAlignment="Stretch"
												VerticalAlignment="Stretch"
												x:Load="{x:Bind NeedsPlaceholderGlyph, Mode=OneWay}"
												Background="{ThemeResource SystemChromeHighColor}"
												CornerRadius="4" />
											<FontIcon
												x:Name="WebShortcutGlyph"
												x:Load="{x:Bind LoadWebShortcutGlyph, Mode=OneWay}"
												Glyph="&#xE71B;" />
											<Image
												x:Name="IconOverlay"
												Width="16"
												Height="16"
												Margin="2"
												HorizontalAlignment="Left"
												VerticalAlignment="Bottom"
												x:Load="True"
												Source="{x:Bind IconOverlay, Mode=OneWay}"
												Stretch="Uniform" />
											<Border
												x:Name="ShortcutGlyphElement"
												HorizontalAlignment="Left"
												VerticalAlignment="Bottom"
												x:Load="{x:Bind IsShortcut}">
												<uc:OpacityIcon Style="{StaticResource ColorIconShortcut}" />
											</Border>
											<Image
												x:Name="ShieldOverlay"
												Width="8"
												Height="8"
												HorizontalAlignment="Right"
												VerticalAlignment="Bottom"
												x:Load="{x:Bind IsElevated}"
												Source="{x:Bind ShieldIcon, Mode=OneWay}"
												Stretch="Uniform" />
										</Grid>

										<!--  Selection Check Box  -->
										<CheckBox
											x:Name="SelectionCheckbox"
											Width="20"
											MinWidth="0"
											HorizontalAlignment="Center"
											AutomationProperties.AccessibilityView="Raw"
											Checked="ItemSelected_Checked"
											DoubleTapped="SelectionCheckbox_DoubleTapped"
											Opacity="0"
											Unchecked="ItemSelected_Unchecked" />

									</Grid>

									<!--  Item Name  -->
									<Grid
										Width="{Binding Path=DataContext.ColumnsViewModel.NameColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										Padding="12,0,0,0"
										HorizontalAlignment="Left"
										VerticalAlignment="Center"
										Canvas.ZIndex="6">

										<TextBlock
											x:Name="ItemName"
											Padding="6,0,0,0"
											VerticalAlignment="Center"
											DataContextChanged="TextBlock_DataContextChanged"
											IsTextTrimmedChanged="TextBlock_IsTextTrimmedChanged"
											Opacity="{x:Bind Opacity, Mode=OneWay}"
											Text="{x:Bind Name, Mode=OneWay}"
											TextTrimming="CharacterEllipsis" />

										<TextBox
											x:Name="ItemNameTextBox"
											MaxWidth="{Binding MaxWidthForRenameTextbox, ElementName=PageRoot, Mode=OneWay}"
											HorizontalAlignment="Left"
											VerticalAlignment="Center"
											BeforeTextChanging="ItemNameTextBox_BeforeTextChanging"
											Visibility="Collapsed">
											<TextBox.Resources>
												<SolidColorBrush x:Key="TextControlBackgroundFocused" Color="{ThemeResource SolidBackgroundFillColorSecondary}" />
											</TextBox.Resources>
										</TextBox>

									</Grid>

									<!--  Item Git Status  -->
									<Border
										x:Name="ItemGitStatusTextBlock"
										Width="{Binding Path=DataContext.ColumnsViewModel.GitStatusColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										Padding="10,0,0,0"
										HorizontalAlignment="Stretch"
										VerticalAlignment="Center"
										x:Load="{x:Bind IsGitItem}">
										<uc:OpacityIcon
											Width="16"
											Height="16"
											HorizontalAlignment="Left"
											Style="{x:Bind AsGitItem.UnmergedGitStatusIcon, Mode=OneWay}"
											ToolTipService.ToolTip="{x:Bind AsGitItem.UnmergedGitStatusName, Mode=OneWay}"
											Visibility="{Binding Path=DataContext.ColumnsViewModel.GitStatusColumn.Visibility, ElementName=PageRoot, Mode=OneWay}" />
									</Border>

									<!--  Item Git Last Commit Date  -->
									<TextBlock
										x:Name="ItemGitLastCommitDateTextBlock"
										Width="{Binding Path=DataContext.ColumnsViewModel.GitLastCommitDateColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										x:Load="{x:Bind IsGitItem}"
										DataContextChanged="TextBlock_DataContextChanged"
										IsTextTrimmedChanged="TextBlock_IsTextTrimmedChanged"
										Style="{StaticResource ColumnContentTextBlock}"
										Text="{x:Bind AsGitItem.GitLastCommitDateHumanized, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.GitLastCommitDateColumn.Visibility, ElementName=PageRoot, Mode=OneWay}" />

									<!--  Item Git Last Commit Message  -->
									<TextBlock
										x:Name="ItemGitLastCommitMessageTextBlock"
										Width="{Binding Path=DataContext.ColumnsViewModel.GitLastCommitMessageColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										x:Load="{x:Bind IsGitItem}"
										DataContextChanged="TextBlock_DataContextChanged"
										IsTextTrimmedChanged="TextBlock_IsTextTrimmedChanged"
										Style="{StaticResource ColumnContentTextBlock}"
										Text="{x:Bind AsGitItem.GitLastCommitMessage, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.GitLastCommitMessageColumn.Visibility, ElementName=PageRoot, Mode=OneWay}" />

									<!--  Item Git Last Commit Author  -->
									<TextBlock
										x:Name="ItemGitCommitAuthorTextBlock"
										Width="{Binding Path=DataContext.ColumnsViewModel.GitCommitAuthorColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										x:Load="{x:Bind IsGitItem}"
										DataContextChanged="TextBlock_DataContextChanged"
										IsTextTrimmedChanged="TextBlock_IsTextTrimmedChanged"
										Style="{StaticResource ColumnContentTextBlock}"
										Text="{x:Bind AsGitItem.GitLastCommitAuthor, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.GitCommitAuthorColumn.Visibility, ElementName=PageRoot, Mode=OneWay}" />

									<!--  Item Git Last Commit SHA  -->
									<TextBlock
										x:Name="ItemGitLastCommitShaTextBlock"
										Width="{Binding Path=DataContext.ColumnsViewModel.GitLastCommitShaColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										x:Load="{x:Bind IsGitItem}"
										Style="{StaticResource ColumnContentTextBlock}"
										Text="{x:Bind AsGitItem.GitLastCommitSha, Mode=OneWay}"
										ToolTipService.ToolTip="{x:Bind AsGitItem.GitLastCommitFullSha, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.GitLastCommitShaColumn.Visibility, ElementName=PageRoot, Mode=OneWay}" />

									<!--  Tags That Item Is Labeled  -->
									<Grid
										x:Name="ItemTagGrid"
										Width="{Binding Path=DataContext.ColumnsViewModel.TagColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										Padding="12,0,12,0"
										HorizontalAlignment="Stretch"
										VerticalAlignment="Center"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.TagColumn.Visibility, ElementName=PageRoot, Mode=OneWay}">
										<ItemsRepeater
											x:Name="TagsRepeater"
											x:Load="{x:Bind HasTags, Mode=OneWay}"
											ItemsSource="{x:Bind FileTagsUI, Mode=OneWay}"
											Visibility="{x:Bind FileTagsUI, Converter={StaticResource EmptyObjectToObjectConverter}, Mode=OneWay}">
											<ItemsRepeater.Layout>
												<StackLayout Orientation="Horizontal" Spacing="4" />
											</ItemsRepeater.Layout>
											<ItemsRepeater.ItemTemplate>
												<DataTemplate x:DataType="filetags:TagViewModel">
													<UserControl PointerEntered="FileTag_PointerEntered" PointerExited="FileTag_PointerExited">
														<StackPanel
															Height="24"
															Padding="8,0"
															HorizontalAlignment="Left"
															x:Phase="2"
															BorderBrush="{x:Bind Color, Mode=OneWay, Converter={StaticResource StringToBrushConverter}}"
															BorderThickness="1"
															CornerRadius="12"
															Orientation="Horizontal"
															Tapped="TagItem_Tapped"
															ToolTipService.ToolTip="{x:Bind Name, Mode=OneWay}">
															<FontIcon
																x:Name="TagIcon"
																FontSize="12"
																Foreground="{x:Bind Color, Mode=OneWay, Converter={StaticResource StringToBrushConverter}}"
																Glyph="&#xE8EC;"
																Tapped="TagIcon_Tapped"
																ToolTipService.ToolTip="{helpers:ResourceString Name=Remove}" />
															<TextBlock
																MaxWidth="200"
																Padding="8,0,0,0"
																VerticalAlignment="Center"
																FontSize="12"
																LineHeight="18"
																Style="{StaticResource ColumnContentTextBlock}"
																Text="{x:Bind Name, Mode=OneWay}"
																TextTrimming="CharacterEllipsis" />

															<VisualStateManager.VisualStateGroups>
																<VisualStateGroup x:Name="CommonStates">
																	<VisualState x:Name="Normal" />

																	<VisualState x:Name="PointerOver">
																		<VisualState.Setters>
																			<Setter Target="TagIcon.Glyph" Value="&#xE711;" />
																		</VisualState.Setters>
																	</VisualState>
																</VisualStateGroup>
															</VisualStateManager.VisualStateGroups>
														</StackPanel>
													</UserControl>
												</DataTemplate>
											</ItemsRepeater.ItemTemplate>
										</ItemsRepeater>
									</Grid>

									<!--  Item Path  -->
									<TextBlock
										x:Name="ItemPath"
										Width="{Binding Path=DataContext.ColumnsViewModel.PathColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										DataContextChanged="TextBlock_DataContextChanged"
										IsTextTrimmedChanged="TextBlock_IsTextTrimmedChanged"
										Style="{StaticResource ColumnContentTextBlock}"
										Text="{x:Bind ItemPath, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.PathColumn.Visibility, ElementName=PageRoot, Mode=OneWay}">
										<!--<ToolTipService.ToolTip>
											<ToolTip Content="{Binding ItemPath.Text, Mode=OneWay}" IsEnabled="{Binding ItemPath.IsTextTrimmed, Mode=OneWay}" />
										</ToolTipService.ToolTip>-->
									</TextBlock>

									<!--  Item Original Path  -->
									<TextBlock
										x:Name="ItemOriginalPath"
										Width="{Binding Path=DataContext.ColumnsViewModel.OriginalPathColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										x:Load="{x:Bind IsRecycleBinItem}"
										DataContextChanged="TextBlock_DataContextChanged"
										IsTextTrimmedChanged="TextBlock_IsTextTrimmedChanged"
										Style="{StaticResource ColumnContentTextBlock}"
										Text="{x:Bind AsRecycleBinItem.ItemOriginalPath, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.OriginalPathColumn.Visibility, ElementName=PageRoot, Mode=OneWay}" />

									<!--  Item Deleted Date  -->
									<TextBlock
										x:Name="ItemDateDeleted"
										Width="{Binding Path=DataContext.ColumnsViewModel.DateDeletedColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										x:Load="{x:Bind IsRecycleBinItem}"
										DataContextChanged="TextBlock_DataContextChanged"
										IsTextTrimmedChanged="TextBlock_IsTextTrimmedChanged"
										Style="{StaticResource ColumnContentTextBlock}"
										Text="{x:Bind AsRecycleBinItem.ItemDateDeleted, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.DateDeletedColumn.Visibility, ElementName=PageRoot, Mode=OneWay}" />

									<!--  Item Modified Date  -->
									<TextBlock
										x:Name="ItemDateModified"
										Width="{Binding Path=DataContext.ColumnsViewModel.DateModifiedColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										DataContextChanged="TextBlock_DataContextChanged"
										IsTextTrimmedChanged="TextBlock_IsTextTrimmedChanged"
										Style="{StaticResource ColumnContentTextBlock}"
										Text="{x:Bind ItemDateModified, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.DateModifiedColumn.Visibility, ElementName=PageRoot, Mode=OneWay}" />

									<!--  Item Created Date  -->
									<TextBlock
										x:Name="ItemDateCreated"
										Width="{Binding Path=DataContext.ColumnsViewModel.DateCreatedColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										DataContextChanged="TextBlock_DataContextChanged"
										IsTextTrimmedChanged="TextBlock_IsTextTrimmedChanged"
										Style="{StaticResource ColumnContentTextBlock}"
										Text="{x:Bind ItemDateCreated, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.DateCreatedColumn.Visibility, ElementName=PageRoot, Mode=OneWay}" />

									<!--  Item Type  -->
									<TextBlock
										x:Name="ItemType"
										Width="{Binding Path=DataContext.ColumnsViewModel.ItemTypeColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										DataContextChanged="TextBlock_DataContextChanged"
										IsTextTrimmedChanged="TextBlock_IsTextTrimmedChanged"
										Style="{StaticResource ColumnContentTextBlock}"
										Text="{x:Bind ItemType, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.ItemTypeColumn.Visibility, ElementName=PageRoot, Mode=OneWay}" />

									<!--  Item Size  -->
									<TextBlock
										x:Name="ItemSize"
										Width="{Binding Path=DataContext.ColumnsViewModel.SizeColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										DataContextChanged="TextBlock_DataContextChanged"
										IsTextTrimmedChanged="TextBlock_IsTextTrimmedChanged"
										Style="{StaticResource ColumnContentTextBlock}"
										Text="{x:Bind FileSize, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.SizeColumn.Visibility, ElementName=PageRoot, Mode=OneWay}" />

									<!--  Item Sync Status Badge  -->
									<Border
										x:Name="ItemStatus"
										Width="{Binding Path=DataContext.ColumnsViewModel.StatusColumn.LengthIncludingGridSplitter.Value, ElementName=PageRoot, Mode=OneWay}"
										Padding="10,0,0,0"
										HorizontalAlignment="Stretch"
										VerticalAlignment="Center"
										AutomationProperties.Name="{x:Bind SyncStatusString, Mode=OneWay}"
										Visibility="{Binding Path=DataContext.ColumnsViewModel.StatusColumn.Visibility, ElementName=PageRoot, Mode=OneWay}">
										<uc:OpacityIcon
											Width="16"
											Height="16"
											HorizontalAlignment="Left"
											Style="{x:Bind SyncStatusUI.OpacityIcon, Mode=OneWay}" />
									</Border>

									<!--  Visual Status Managing  -->
									<VisualStateManager.VisualStateGroups>
										<VisualStateGroup x:Name="CheckboxVisibilityStates">
											<VisualState x:Name="HideCheckbox" />
											<VisualState x:Name="ShowCheckbox">
												<VisualState.Setters>
													<Setter Target="SelectionCheckbox.Opacity" Value="1" />
													<Setter Target="IconBox.Visibility" Value="Collapsed" />
												</VisualState.Setters>
											</VisualState>
										</VisualStateGroup>
									</VisualStateManager.VisualStateGroups>
								</StackPanel>
							</UserControl>
						</DataTemplate>
					</ListView.ItemTemplate>

					<!--  ListView Sticky Header  -->
					<i:Interaction.Behaviors>
						<behaviors:StickyHeaderBehavior />
					</i:Interaction.Behaviors>

					<!--  ListView Control Styles  -->
					<ListView.Resources>
						<ResourceDictionary>
							<Style TargetType="ListView">
								<Setter Property="IsTabStop" Value="False" />
								<Setter Property="TabNavigation" Value="Once" />
								<Setter Property="IsSwipeEnabled" Value="True" />
								<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
								<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
								<Setter Property="ScrollViewer.HorizontalScrollMode" Value="Enabled" />
								<Setter Property="ScrollViewer.IsHorizontalRailEnabled" Value="True" />
								<Setter Property="ScrollViewer.VerticalScrollMode" Value="Enabled" />
								<Setter Property="ScrollViewer.IsVerticalRailEnabled" Value="True" />
								<Setter Property="ScrollViewer.ZoomMode" Value="Disabled" />
								<Setter Property="ScrollViewer.IsDeferredScrollingEnabled" Value="False" />
								<Setter Property="ScrollViewer.BringIntoViewOnFocusChange" Value="True" />
								<Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
								<Setter Property="ItemContainerTransitions">
									<Setter.Value>
										<TransitionCollection>
											<AddDeleteThemeTransition />
										</TransitionCollection>
									</Setter.Value>
								</Setter>
								<Setter Property="ItemsPanel">
									<Setter.Value>
										<ItemsPanelTemplate>
											<ItemsStackPanel Orientation="Vertical" />
										</ItemsPanelTemplate>
									</Setter.Value>
								</Setter>
								<Setter Property="Template">
									<Setter.Value>
										<ControlTemplate TargetType="ListView">
											<Border
												Background="{TemplateBinding Background}"
												BorderBrush="{TemplateBinding BorderBrush}"
												BorderThickness="{TemplateBinding BorderThickness}"
												CornerRadius="{TemplateBinding CornerRadius}">
												<ScrollViewer
													x:Name="ScrollViewer"
													HorizontalContentAlignment="Stretch"
													AutomationProperties.AccessibilityView="Raw"
													BringIntoViewOnFocusChange="{TemplateBinding ScrollViewer.BringIntoViewOnFocusChange}"
													HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
													HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
													IsDeferredScrollingEnabled="{TemplateBinding ScrollViewer.IsDeferredScrollingEnabled}"
													IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
													IsHorizontalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsHorizontalScrollChainingEnabled}"
													IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
													IsVerticalScrollChainingEnabled="{TemplateBinding ScrollViewer.IsVerticalScrollChainingEnabled}"
													TabNavigation="{TemplateBinding TabNavigation}"
													VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
													VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
													ZoomMode="{TemplateBinding ScrollViewer.ZoomMode}">
													<Grid>
														<Grid.ColumnDefinitions>
															<ColumnDefinition Width="Auto" />
															<ColumnDefinition Width="*" />
														</Grid.ColumnDefinitions>

														<ItemsPresenter
															Margin="0,0,12,0"
															Padding="{TemplateBinding Padding}"
															HorizontalAlignment="Stretch"
															Footer="{TemplateBinding Footer}"
															FooterTemplate="{TemplateBinding FooterTemplate}"
															FooterTransitions="{TemplateBinding FooterTransitions}"
															Header="{TemplateBinding Header}"
															HeaderTemplate="{TemplateBinding HeaderTemplate}"
															HeaderTransitions="{TemplateBinding HeaderTransitions}" />

													</Grid>
												</ScrollViewer>
											</Border>
										</ControlTemplate>
									</Setter.Value>
								</Setter>
							</Style>

							<ResourceDictionary.ThemeDictionaries>
								<ResourceDictionary x:Key="Default">
									<SolidColorBrush x:Key="GridViewItemCheckBoxBrush" Color="{ThemeResource SystemChromeMediumLowColor}" />
								</ResourceDictionary>
							</ResourceDictionary.ThemeDictionaries>
						</ResourceDictionary>
					</ListView.Resources>

					<!--  ListView Grouping Styles  -->
					<ListView.GroupStyle>
						<GroupStyle>
							<GroupStyle.HeaderTemplate>
								<DataTemplate x:DataType="helpers:IGroupedCollectionHeader">
									<StackPanel
										Margin="0,0,0,4"
										VerticalAlignment="Top"
										AutomationProperties.Name="{x:Bind Model.Text}"
										Spacing="0">
										<StackPanel Orientation="Horizontal" Spacing="4">

											<TextBlock
												VerticalAlignment="Center"
												FontSize="16"
												Style="{StaticResource SubtitleTextBlockStyle}"
												Text="{x:Bind Model.Text, Mode=OneWay}" />

											<TextBlock
												Margin="4,0,0,0"
												VerticalAlignment="Center"
												Style="{StaticResource BodyTextBlockStyle}"
												Text="{x:Bind Model.CountText, Mode=OneWay}"
												Visibility="{x:Bind Model.ShowCountTextBelow, Mode=OneWay, Converter={StaticResource NegatedBoolToVisibilityConverter}}" />

											<TextBlock
												VerticalAlignment="Center"
												FontSize="14"
												Style="{StaticResource SubheaderTextBlockStyle}"
												Text="{x:Bind Model.Subtext, Mode=OneWay}"
												Visibility="{x:Bind Model.ShowCountTextBelow, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
										</StackPanel>

										<TextBlock
											Margin="0,2,0,0"
											Style="{StaticResource BodyTextBlockStyle}"
											Text="{x:Bind Model.CountText, Mode=OneWay}"
											Visibility="{x:Bind Model.ShowCountTextBelow, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

									</StackPanel>
								</DataTemplate>
							</GroupStyle.HeaderTemplate>
						</GroupStyle>
					</ListView.GroupStyle>

					<!--  ListView Control Property Customizing  -->
					<ListView.ItemContainerStyle>
						<Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
							<Setter Property="HorizontalContentAlignment" Value="Stretch" />
							<Setter Property="MinHeight" Value="1" />
							<Setter Property="Height" Value="{ThemeResource ListItemHeight}" />
						</Style>
					</ListView.ItemContainerStyle>

					<!--  ListView Item Displaying Style  -->
					<ListView.ItemsPanel>
						<ItemsPanelTemplate>
							<ItemsStackPanel AreStickyGroupHeadersEnabled="False" Orientation="Vertical" />
						</ItemsPanelTemplate>
					</ListView.ItemsPanel>

				</ListView>
			</SemanticZoom.ZoomedInView>

			<!--  Grouped View  -->
			<SemanticZoom.ZoomedOutView>
				<ListView
					HorizontalAlignment="Stretch"
					ItemsSource="{x:Bind CollectionViewSource.View.CollectionGroups, Mode=OneWay}"
					SelectionMode="None">

					<ListView.ItemContainerTransitions>
						<TransitionCollection>
							<ContentThemeTransition />
						</TransitionCollection>
					</ListView.ItemContainerTransitions>

					<ListView.ItemTemplate>
						<DataTemplate x:DataType="ICollectionViewGroup">
							<StackPanel
								Margin="0,4,0,4"
								HorizontalAlignment="Stretch"
								VerticalAlignment="Center"
								Orientation="Horizontal"
								Spacing="4">
								<Image
									Height="36"
									Source="{x:Bind ((helpers:IGroupedCollectionHeader)Group).Model.ImageSource, Mode=OneWay}"
									Stretch="Uniform" />
								<FontIcon FontSize="32" Glyph="{x:Bind ((helpers:IGroupedCollectionHeader)Group).Model.Icon, Mode=OneWay}" />
								<StackPanel
									VerticalAlignment="Center"
									AutomationProperties.Name="{x:Bind ((helpers:IGroupedCollectionHeader)Group).Model.Text, Mode=OneWay}"
									Orientation="Vertical">
									<StackPanel Orientation="Horizontal" Spacing="4">
										<TextBlock
											VerticalAlignment="Center"
											FontSize="16"
											Style="{StaticResource SubtitleTextBlockStyle}"
											Text="{x:Bind ((helpers:IGroupedCollectionHeader)Group).Model.Text, Mode=OneWay}" />

										<TextBlock
											Margin="4,0,0,0"
											VerticalAlignment="Center"
											Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"
											Style="{StaticResource BodyTextBlockStyle}"
											Text="{x:Bind ((helpers:IGroupedCollectionHeader)Group).Model.CountText, Mode=OneWay}"
											Visibility="{x:Bind ((helpers:IGroupedCollectionHeader)Group).Model.ShowCountTextBelow, Mode=OneWay, Converter={StaticResource NegatedBoolToVisibilityConverter}}" />

										<TextBlock
											VerticalAlignment="Center"
											FontSize="14"
											Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"
											Style="{StaticResource SubheaderTextBlockStyle}"
											Text="{x:Bind ((helpers:IGroupedCollectionHeader)Group).Model.Subtext, Mode=OneWay}"
											Visibility="{x:Bind ((helpers:IGroupedCollectionHeader)Group).Model.ShowCountTextBelow, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
									</StackPanel>
									<TextBlock
										Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"
										Style="{StaticResource BodyTextBlockStyle}"
										Text="{x:Bind ((helpers:IGroupedCollectionHeader)Group).Model.CountText, Mode=OneWay}"
										Visibility="{x:Bind ((helpers:IGroupedCollectionHeader)Group).Model.ShowCountTextBelow, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
								</StackPanel>
							</StackPanel>
						</DataTemplate>
					</ListView.ItemTemplate>

					<ListView.ItemContainerStyle>
						<Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
							<Setter Property="HorizontalContentAlignment" Value="Stretch" />
						</Style>
					</ListView.ItemContainerStyle>

				</ListView>
			</SemanticZoom.ZoomedOutView>

		</SemanticZoom>

		<!--  Search Unindexed Items  -->
		<StackPanel
			x:Name="SearchUnindexedItemsPanel"
			HorizontalAlignment="Center"
			VerticalAlignment="Bottom"
			x:Load="{x:Bind Commands.SearchUnindexedItems.IsExecutable, Mode=OneWay}"
			Orientation="Vertical">
			<TextBlock Text="{helpers:ResourceString Name=SearchUnindexedItemsLabel/Text}" />
			<HyperlinkButton
				x:Name="SearchUnindexedItemsButton"
				HorizontalAlignment="Center"
				Command="{x:Bind Commands.SearchUnindexedItems}"
				Content="{x:Bind Commands.SearchUnindexedItems.Label}" />
		</StackPanel>

		<!--  Selector  -->
		<Canvas HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
			<Rectangle
				Name="SelectionRectangle"
				Canvas.Left="0"
				Canvas.Top="0"
				Width="0"
				Height="0"
				Fill="{ThemeResource SystemAccentColor}"
				Opacity=".5"
				Stroke="{ThemeResource SystemAccentColorLight1}"
				StrokeThickness="1" />
		</Canvas>

	</Grid>
</local:StandardViewBase>
