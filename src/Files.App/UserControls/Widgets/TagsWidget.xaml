<UserControl
    x:Class="Files.App.UserControls.Widgets.TagsWidget"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Files.App.UserControls.Widgets"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Files.Backend.ViewModels.Widgets.FileTagsWidget"
    d:DesignHeight="300"
    xmlns:vc="using:Files.App.ValueConverters"
    d:DesignWidth="400"
    mc:Ignorable="d">

    <UserControl.Resources>
        <vc:ColorModelToColorConverter x:Key="ColorModelToColorConverter"/>
    </UserControl.Resources>

    <Grid>
        <!--  Collection of containers  -->
        <GridView ItemsSource="{x:Bind ViewModel.Containers}">

            <!--  Container template  -->
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="vm:FileTagsContainerViewModel">
                    <Grid
                        Width="256"
                        Height="302"
                        AllowDrop="True"
                        Background="{ThemeResource ControlFillColorDefaultBrush}"
                        ColumnSpacing="8"
                        CornerRadius="{StaticResource ControlCornerRadius}">
                        <Grid.RowDefinitions>
                            <!-- Title -->
                            <RowDefinition Height="Auto" />
                            <!-- Contents -->
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <!-- Title -->
                        <StackPanel Grid.Row="0" Spacing="8">
                            <Ellipse Fill="{x:Bind TagColor, Converter={StaticResource ColorModelToColorConverter}}"></Ellipse>
                        </StackPanel>

                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</UserControl>
