<!--  Copyright (c) 2023 Files Community. Licensed under the MIT License. See the LICENSE.  -->
<ContentDialog
	x:Class="Files.App.Dialogs.ReloadJsonParseErrorDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:helpers="using:Files.App.Helpers"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Title="Failed to reload settings"
	PrimaryButtonText="Reset the settings"
	RequestedTheme="{x:Bind helpers:ThemeHelper.RootTheme}"
	SecondaryButtonStyle="{StaticResource AccentButtonStyle}"
	SecondaryButtonText="Close the app"
	Style="{StaticResource DefaultContentDialogStyle}"
	mc:Ignorable="d">

	<StackPanel>

		<TextBlock Margin="0,0,0,20" Text="Settings could not be reloaded from file. Check for syntax errors, including trailing commas." />

		<TextBlock Text="Syntax error:" />
		<TextBlock Foreground="{ThemeResource SystemFillColorCaution}" Text="* Line 42, Column 3..." />

	</StackPanel>
</ContentDialog>
