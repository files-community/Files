<ContentDialog
	x:Class="Files.App.Dialogs.AddItemDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:helpers="using:Files.App.Helpers"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vc="using:Files.App.ValueConverters"
	xmlns:vm="using:Files.Backend.ViewModels.Dialogs.AddItemDialog"
	Title="{helpers:ResourceString Name=AddDialog/Title}"
	Loaded="AddItemDialog_Loaded"
	RequestedTheme="{x:Bind helpers:ThemeHelper.RootTheme}"
	SecondaryButtonText="{helpers:ResourceString Name=Cancel}"
	Style="{StaticResource DefaultContentDialogStyle}"
	mc:Ignorable="d">

	<ContentDialog.Resources>
		<vc:ImageModelToImageConverter x:Key="ImageModelToImageConverter" />
	</ContentDialog.Resources>

	<Grid x:Name="RootGrid" RowSpacing="24">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!--  Description  -->
		<TextBlock
			x:Name="DescriptionTextBlock"
			Grid.Row="0"
			Text="{helpers:ResourceString Name=AddDialogDescription/Text}" />

		<!--  Adding items list  -->
		<Grid x:Name="SelectionListContent" Grid.Row="1">
			<ListView
				x:Name="AddItemsListView"
				Width="400"
				IsItemClickEnabled="True"
				ItemClick="ListView_ItemClick"
				ItemsSource="{x:Bind ViewModel.AddItemsList, Mode=OneWay}">
				<ListView.ItemTemplate>
					<DataTemplate x:DataType="vm:AddItemDialogListItemViewModel">
						<Grid Height="48">

							<StackPanel VerticalAlignment="Center" Orientation="Horizontal">

								<!--  Object image  -->
								<Grid Margin="0,0,10,0" VerticalAlignment="Center">
									<Viewbox
										x:Name="IconRoot"
										Width="24"
										Height="24"
										HorizontalAlignment="Left"
										VerticalAlignment="Center">
										<FontIcon Glyph="{x:Bind Glyph}" />
									</Viewbox>
									<Image
										Width="24"
										Height="24"
										Margin="0"
										HorizontalAlignment="Left"
										VerticalAlignment="Center"
										Source="{x:Bind Icon, Converter={StaticResource ImageModelToImageConverter}}"
										Stretch="Uniform" />
								</Grid>

								<!--  Object name  -->
								<StackPanel>
									<TextBlock Text="{x:Bind Header}" />
									<TextBlock Foreground="Gray" Text="{x:Bind SubHeader}" />
								</StackPanel>

							</StackPanel>

						</Grid>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</Grid>

	</Grid>
</ContentDialog>
