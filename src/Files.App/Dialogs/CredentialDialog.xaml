<!--  Copyright (c) 2023 Files Community. Licensed under the MIT License. See the LICENSE.  -->
<ContentDialog
	x:Class="Files.App.Dialogs.CredentialDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:converters="using:CommunityToolkit.WinUI.UI.Converters"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:helpers="using:Files.App.Helpers"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Title="{helpers:ResourceString Name=AskCredentialDialog/Title}"
	CornerRadius="{StaticResource OverlayCornerRadius}"
	DataContext="{x:Bind ViewModel, Mode=OneWay}"
	PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
	PrimaryButtonStyle="{StaticResource AccentButtonStyle}"
	PrimaryButtonText="{helpers:ResourceString Name=OK}"
	RequestedTheme="{x:Bind helpers:ThemeHelper.RootTheme}"
	SecondaryButtonText="{helpers:ResourceString Name=Cancel}"
	Style="{StaticResource DefaultContentDialogStyle}"
	mc:Ignorable="d">

	<ContentDialog.Resources>
		<ResourceDictionary>
			<converters:BoolNegationConverter x:Key="BoolNegationConverter" />
		</ResourceDictionary>
	</ContentDialog.Resources>

	<Grid x:Name="RootGrid" RowSpacing="24">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!--  Description  -->
		<TextBlock
			x:Name="Description"
			Grid.Row="0"
			Text="{helpers:ResourceString Name=CredentialDialogDescription/Text}" />

		<StackPanel
			Grid.Row="1"
			HorizontalAlignment="Stretch"
			Spacing="8">

			<!--  User Name  -->
			<TextBox
				IsEnabled="{x:Bind ViewModel.IsAnonymous, Converter={StaticResource BoolNegationConverter}, Mode=OneWay}"
				PlaceholderText="{helpers:ResourceString Name=CredentialDialogUserName/PlaceholderText}"
				Text="{x:Bind ViewModel.UserName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

			<!--  Password  -->
			<PasswordBox
				x:Name="CredentialPasswordBox"
				IsEnabled="{x:Bind ViewModel.IsAnonymous, Converter={StaticResource BoolNegationConverter}, Mode=OneWay}"
				PlaceholderText="{helpers:ResourceString Name=Password}" />

			<!--  Is Anonymous  -->
			<CheckBox x:Name="Anonymous" Content="{helpers:ResourceString Name=CredentialDialogAnonymous/Content}" />

		</StackPanel>
	</Grid>
</ContentDialog>