<ContentDialog
	x:Class="Files.App.Dialogs.CreateShortcutDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:converters="using:CommunityToolkit.WinUI.UI.Converters"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:helpers="using:Files.App.Helpers"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Title="{helpers:ResourceString Name=NewShortcutDialogTitle}"
	DefaultButton="Primary"
	PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
	PrimaryButtonText="{helpers:ResourceString Name=Continue}"
	RequestedTheme="{x:Bind helpers:ThemeHelper.RootTheme}"
	SecondaryButtonText="{helpers:ResourceString Name=Cancel}"
	Style="{StaticResource DefaultContentDialogStyle}"
	mc:Ignorable="d">

	<ContentDialog.Resources>
		<ResourceDictionary>
			<converters:BoolToVisibilityConverter
				x:Key="NegatedBoolToVisibilityConverter"
				FalseValue="Visible"
				TrueValue="Collapsed" />
		</ResourceDictionary>
	</ContentDialog.Resources>
	
	<Border Width="400">
		<Grid
			x:Name="DestinationPathGrid"
			ColumnSpacing="8"
			RowSpacing="8">
			<Grid.ColumnDefinitions>
				<ColumnDefinition />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>
			
			<!--  Header  -->
			<TextBlock 
				Grid.ColumnSpan="2"
				Margin="0, 0, 0, 20"
				Text="{helpers:ResourceString Name=NewShortcutDialogDescription}"
				TextWrapping="Wrap" />

			<TextBlock
				Grid.Row="1"
				Grid.ColumnSpan="2"
				Text="{helpers:ResourceString Name=NewShortcutDialogPrompt}" />

			<!--  Path Box  -->
			<TextBox
				x:Name="DestinationItemPath"
				Grid.Row="2"
				Grid.Column="0"
				GotFocus="DestinationItemPath_GotFocus"
				HorizontalAlignment="Stretch"
				PlaceholderText="C:\Users\"
				Text="{x:Bind ViewModel.DestinationItemPath, Mode=TwoWay}" />
			<Button
				x:Name="SelectDestination"
				Grid.Row="2"
				Grid.Column="1"
				Command="{x:Bind ViewModel.SelectDestinationCommand}"
				Content="{helpers:ResourceString Name=Browse}" />

			<TextBlock
				x:Name="LocationErrorBox"
				Grid.Row="3"
				Grid.ColumnSpan="2"
				FontStyle="Italic"
				Text="{helpers:ResourceString Name=ShortcutLocationError}"
				Visibility="{x:Bind ViewModel.IsLocationValid, Mode=OneWay, Converter={StaticResource NegatedBoolToVisibilityConverter}}" />
		</Grid>
	</Border>
</ContentDialog>