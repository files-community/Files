namespace Files.Backend.Item
{
    // This class will be removed when the service becomes available in Files.Shared.
    internal static class LocalizationExtension
    {
        public static string ToLocalized(this string key) => key;
    }
}
