<UserControl
    x:Class="Files.UserControls.SearchPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="using:Files.Helpers"
    xmlns:converters="using:Files.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
    MinWidth="180" MaxWidth="360" mc:Ignorable="d">

    <UserControl.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </UserControl.Resources>

    <Grid x:Name="RootPanel" Margin="8" VerticalAlignment="Stretch" RowSpacing="24" ColumnSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Frame x:Name="MenuFrame" Grid.ColumnSpan="2" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />
        <Button x:Name="SearchButton" Grid.Row="1" Command="{x:Bind Navigator.SearchCommand}"
            HorizontalAlignment="Stretch" Content="{helpers:ResourceString Name=Search}" />
        <Button x:Name="ClearButton" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch"
            Content="{helpers:ResourceString Name=Clear}" Command="{x:Bind Navigator.PageViewModel.Filter.ClearCommand}" />
    </Grid>

</UserControl>