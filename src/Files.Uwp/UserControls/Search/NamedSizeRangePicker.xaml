<UserControl
    x:Class="Files.UserControls.Search.NamedSizeRangePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Files.UserControls.Search"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignWidth="400" d:DesignHeight="300" mc:Ignorable="d">

    <ItemsControl ItemsSource="{x:Bind NamedRanges}">
        <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="local:INamedSizeRange">
                <ToggleButton
                    Margin="0,8,0,0" HorizontalAlignment="Stretch"
                    FontSize="14" FontWeight="SemiBold"
                    IsChecked="{x:Bind IsSelected, Mode=TwoWay}">
                    <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
                        <TextBlock VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Text="{x:Bind Name}" />
                        <TextBlock Grid.Row="1" VerticalAlignment="Center" FontSize="12" Text="{x:Bind Value}" />
                    </StackPanel>
                </ToggleButton>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>

</UserControl>
