<UserControl
    x:Class="Files.UserControls.Search.SizeRangeSlider"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:c="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignWidth="400" d:DesignHeight="300" mc:Ignorable="d">

    <UserControl.Resources>

        <ControlTemplate x:Key="SelectorTemplate" TargetType="c:RangeSelector">
            <Grid x:Name="ControlGrid" Height="18">
                <Border x:Name="OutOfRangeContentContainer" Grid.Column="1" Background="Transparent" CornerRadius="4" Height="4">
                    <Rectangle x:Name="BackgroundElement" Height="4" Fill="{TemplateBinding Background}" />
                </Border>
                <Canvas x:Name="ContainerCanvas" Grid.Column="1" Background="Transparent">
                    <Rectangle x:Name="ActiveRectangle"
                        Height="4" Fill="{TemplateBinding Foreground}"
                        HorizontalAlignment="Stretch" VerticalAlignment="Center" />
                    <Grid x:Name="ToolTip" Loaded="ToolTip_Loaded"
                        Margin="16,-44,0,8" BorderThickness="1" Visibility="Collapsed"
                        Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                        BorderBrush="{ThemeResource SystemControlForegroundChromeHighBrush}">
                        <TextBlock x:Name="ToolTipText" Text="{x:Bind Tag, Mode=TwoWay}" Visibility="Collapsed" />
                        <TextBlock x:Name="ToolTipLabel" Loaded="ToolTipBlock_Loaded"
                            Margin="8" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" />
                    </Grid>
                    <Thumb x:Name="MinThumb"
                        TabIndex="0" IsTabStop="True"
                        AutomationProperties.Name="Min thumb"
                        Style="{StaticResource SliderThumbStyle}" />
                    <Thumb x:Name="MaxThumb"
                        TabIndex="1" IsTabStop="True"
                        AutomationProperties.Name="Max thumb"
                        Style="{StaticResource SliderThumbStyle}" />
                </Canvas>
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="MinPressed">
                            <Storyboard>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="MinThumb" Storyboard.TargetProperty="Background">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlHighlightChromeHighBrush}" />
                                </ObjectAnimationUsingKeyFrames>
                            </Storyboard>
                        </VisualState>
                        <VisualState x:Name="MaxPressed">
                            <Storyboard>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="MaxThumb" Storyboard.TargetProperty="Background">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlHighlightChromeHighBrush}" />
                                </ObjectAnimationUsingKeyFrames>
                            </Storyboard>
                        </VisualState>
                        <VisualState x:Name="Disabled">
                            <Storyboard>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundElement" Storyboard.TargetProperty="Fill">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlDisabledBaseLowBrush}" />
                                </ObjectAnimationUsingKeyFrames>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ActiveRectangle" Storyboard.TargetProperty="Fill">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlDisabledBaseLowBrush}" />
                                </ObjectAnimationUsingKeyFrames>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="MinThumb" Storyboard.TargetProperty="Background">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlDisabledChromeDisabledHighBrush}" />
                                </ObjectAnimationUsingKeyFrames>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="MaxThumb" Storyboard.TargetProperty="Background">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlDisabledChromeDisabledHighBrush}" />
                                </ObjectAnimationUsingKeyFrames>
                            </Storyboard>
                        </VisualState>
                        <VisualState x:Name="PointerOver">
                            <Storyboard>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BackgroundElement" Storyboard.TargetProperty="Fill">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                                </ObjectAnimationUsingKeyFrames>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="MinThumb" Storyboard.TargetProperty="Background">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlHighlightChromeAltLowBrush}" />
                                </ObjectAnimationUsingKeyFrames>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="MaxThumb" Storyboard.TargetProperty="Background">
                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlHighlightChromeAltLowBrush}" />
                                </ObjectAnimationUsingKeyFrames>
                            </Storyboard>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
            </Grid>
        </ControlTemplate>

        <Style x:Key="SliderThumbStyle" TargetType="Thumb">
            <Setter Property="UseSystemFocusVisuals" Value="True" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="{ThemeResource SliderThumbBackground}" />
            <Setter Property="Width" Value="18" />
            <Setter Property="Height" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border
                            BorderThickness="3" CornerRadius="15"
                            Background="{ThemeResource SliderThumbBackground}"
                            BorderBrush="{ThemeResource SolidBackgroundAcrylic}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <c:RangeSelector x:Name="Selector"
        Minimum="0" StepFrequency="3"
        Tag="{x:Bind ToolTip, Mode=TwoWay}"
        Template="{StaticResource SelectorTemplate}" />

</UserControl>
