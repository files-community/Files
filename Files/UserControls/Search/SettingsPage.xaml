<Page
    x:Class="Files.UserControls.Search.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Files.UserControls.Search"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical">
        <TextBlock Text="Search Options" Margin="0,0,0,8" FontSize="18" FontWeight="SemiBold" />
        <local:Picker ViewModel="{x:Bind ViewModel.PickerViewModel}" />
        <StackPanel Orientation="Vertical" Spacing="4">
            <TextBlock FontSize="14" FontWeight="SemiBold" Text="Search in" />
            <CheckBox Content="Subfolders" IsChecked="{x:Bind ViewModel.LocationViewModel.UseSubFolders, Mode=TwoWay}" />
            <CheckBox Content="System files" IsChecked="{x:Bind ViewModel.LocationViewModel.UseSystemFiles, Mode=TwoWay}" />
            <CheckBox Content="Compressed files" IsChecked="{x:Bind ViewModel.LocationViewModel.UseCompressedFiles, Mode=TwoWay}" />
        </StackPanel>
        <HyperlinkButton Content="Search &amp; indexing options…" />
        <Grid Width="200" ColumnSpacing="8" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" HorizontalAlignment="Stretch" Content="Search" />
            <Button Grid.Column="1" Content="Default" HorizontalAlignment="Stretch" />
        </Grid>
    </StackPanel>
</Page>
