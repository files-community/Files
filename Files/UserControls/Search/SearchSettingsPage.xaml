<Page
    x:Class="Files.UserControls.Search.SearchSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Files.UserControls.Search"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock x:Uid="SearchSettingOptions_Title" FontSize="18" FontWeight="SemiBold" Text="Search Options" />
        <local:Picker Grid.Row="1" ViewModel="{x:Bind ViewModel.FilterViewModel}" />
        <local:Picker Grid.Row="2" ViewModel="{x:Bind ViewModel.LocationViewModel}" />
        <Grid Grid.Row="3" Margin="0,24,0,0" HorizontalAlignment="Stretch" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button x:Name="SearchButton" x:Uid="SearchSettingsPage_Search"
                HorizontalAlignment="Stretch" Content="Search" Command="{x:Bind ViewModel.SearchCommand}" />
        </Grid>
    </Grid>

</Page>
