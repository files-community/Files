<UserControl x:Class="Files.UserControls.DetailsPane" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:Files.UserControls" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls" 
             xmlns:Interactivity="using:Microsoft.Xaml.Interactivity" 
             xmlns:Core="using:Microsoft.Xaml.Interactions.Core" 
             xmlns:triggers="using:Microsoft.Toolkit.Uwp.UI.Triggers" 
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="400" MinWidth="90" >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ResourceDictionaries/DefaultGridSplitterStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="RootGrid" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" x:Name="PropertiesRow"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" x:Name="PropertiesColumn"/>
        </Grid.ColumnDefinitions>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers >
                        <triggers:IsEqualStateTrigger Value="{x:Bind isHorizontalInternal, Mode=OneWay}" To="True"/>
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="RootPropertiesGrid.(Grid.Row)" Value="0"/>
                        <Setter Target="RootPropertiesGrid.(Grid.Column)" Value="2"/>
                        <Setter Target="HorizontalGridSplitter.(Grid.Row)" Value="0"/>
                        <Setter Target="HorizontalGridSplitter.(Grid.Column)" Value="1"/>
                        <Setter Target="PropertiesRow.Height" Value="0"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers >
                        <triggers:IsEqualStateTrigger Value="{x:Bind isHorizontalInternal, Mode=OneWay}" To="False"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RootPropertiesGrid.(Grid.Row)" Value="2"/>
                        <Setter Target="RootPropertiesGrid.(Grid.Column)" Value="0"/>
                        <Setter Target="HorizontalGridSplitter.(Grid.Row)" Value="1"/>
                        <Setter Target="HorizontalGridSplitter.(Grid.Column)" Value="0"/>
                        <Setter Target="PropertiesColumn.Width" Value="0"/>
                    </VisualState.Setters>
                </VisualState>

                <VisualState>
                    
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid Grid.Row="0" x:Name="RootPreviewGrid" VerticalAlignment="Center">
            <TextBlock x:Name="PreviewNotAvaliableText" Visibility="Collapsed" Text="No preview avaliable"/>
            <Grid x:Name="PreviewGrid">
            </Grid>
        </Grid>

        <controls:GridSplitter Margin="5, 5, 5, 5" Grid.Row="1" Grid.Column="0" x:Name="HorizontalGridSplitter" Style="{StaticResource PaneGridSplitter}"/>

        <ListView SelectionMode="None" Grid.Row="2" x:Name="RootPropertiesGrid">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <FontIcon FontFamily="{StaticResource FluentUIGlyphs}" FontSize="24" Glyph="&#xE922;" Grid.Column="0" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="260x260" VerticalAlignment="Center" />
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="15" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <FontIcon FontFamily="{StaticResource FluentUIGlyphs}" VerticalAlignment="Center" FontSize="24" Glyph="&#xE972;" Grid.Column="0" />

                <StackPanel Orientation="Vertical" Grid.Column="2">
                </StackPanel>
            </Grid>
        </ListView>

    </Grid>
</UserControl>
