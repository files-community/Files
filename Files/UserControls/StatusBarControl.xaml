<UserControl
    x:Class="Files.UserControls.StatusBarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Files.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="40"
    d:DesignWidth="400">

    <Grid Height="40" HorizontalAlignment="Stretch" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Padding="2" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button
                Width="34"
                Height="30"
                Margin="2,0,2,0"
                Background="Transparent"
                CornerRadius="2" FontSize="16"
                FontFamily="Segoe MDL2 Assets"
                Style="{StaticResource ToolBarButtonStyle}">
                <Button.Content>
                    &#xE762;
                </Button.Content>
            </Button>

            <ToggleButton
                Width="34"
                Height="30" FontSize="16"
                Margin="2,0,2,0"
                Background="Transparent"
                CornerRadius="2"
                FontFamily="Segoe MDL2 Assets">
                <ToggleButton.Content>
                    &#xF0E2;
                </ToggleButton.Content>
            </ToggleButton>
        </StackPanel>

    </Grid>
</UserControl>
