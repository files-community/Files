namespace Files.Enums
{
    public enum SortOption : byte
    {
        Name,
        DateModified,
        Size,
        FileType
    }

    public enum SidebarSortOption : byte
    {
        Name,
        DateAdded,
        Custom
    }
}