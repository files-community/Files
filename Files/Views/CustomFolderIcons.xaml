<Page
    x:Class="Files.Views.CustomFolderIcons"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Files.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:common="using:Files.Common"
    Background="Transparent"
    MaxWidth="372">

    <Grid >
        <StackPanel Orientation="Vertical">
            <TextBlock Padding="0,12" FontWeight="SemiBold" Text="Pick a custom folder icon"/>
            <Grid ColumnSpacing="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" x:Name="ItemDisplayedPath" IsReadOnly="True" />
                <Button Grid.Column="1" x:Name="PickDllButton" Click="PickDllButton_Click" Content="Browse..."/>
            </Grid>
            <GridView HorizontalAlignment="Stretch" 
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollMode="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollMode="Disabled" SelectionMode="Single" x:Name="IconSelectionGrid">
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid MaximumRowsOrColumns="4" Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="common:IconFileInfo">
                        <Grid Width="16" Height="16">
                            <Image Source="{x:Bind ((BitmapImage)ImageSource), Mode=OneWay}" />
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
            <Button Content="Restore default"/>
        </StackPanel>
    </Grid>
</Page>
