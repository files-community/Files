<local:PropertiesTab
    x:Class="Files.Views.PropertiesSecurity"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Files.ViewModels.Properties"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:usercontrols="using:Files.UserControls"
    Loaded="Properties_Loaded"
    mc:Ignorable="d">

    <local:PropertiesTab.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///ResourceDictionaries/PropertiesStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <SolidColorBrush x:Key="ProgressRingBackgroundThemeBrush" Color="#c3c3c3" />
                </ResourceDictionary>

                <ResourceDictionary x:Key="Dark">
                    <SolidColorBrush x:Key="ProgressRingBackgroundThemeBrush" Color="#191919" />
                </ResourceDictionary>

                <ResourceDictionary x:Key="HighContrast">
                    <SolidColorBrush x:Key="ProgressRingBackgroundThemeBrush" Color="#c3c3c3" />
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </local:PropertiesTab.Resources>

    <StackPanel Style="{StaticResource PropertiesTab}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />                
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
        </Grid>

        <Grid Grid.Row="0"
              Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock
                x:Name="ItemUsersGroupLabel"
                x:Uid="ItemUsersGroupLabel"
                Visibility="Visible"
                Grid.Row="0"
                Grid.Column="0"
                Style="{StaticResource PropertyName}"
                FontWeight="Bold"
                Text="Group or user names" />


            
            <Button x:Name="btnAddUserOrGroup"
                    Grid.Column="1"
                    Grid.Row="0"
                    Width="40"
                    Height="40"
                    Content="+"
                    Margin="0,0,80,0"                    
                    HorizontalAlignment="Right"/>

            <Button x:Name="btnRemoveUserOrGroup"
                    Grid.Column="1"
                    Grid.Row="0"
                    Width="40"
                    Height="40"
                    Content="-"
                    Margin="0,0,40,0"
                    HorizontalAlignment="Right"/>
            
            <Button x:Name="btnEditUserOrGroup"
                    Grid.Column="1"
                    Grid.Row="0"
                    Width="40"
                    Height="40"                    
                    Content="&#xE792;"
                    FontFamily="{StaticResource SymbolThemeFontFamily}" 
                    HorizontalAlignment="Right"/>
        </Grid>

        <Grid Grid.Row="1"
              Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <ListView x:Name="lstUserGroups" 
                      Grid.Row="0"
                      Grid.Column="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal">
                                <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE716;" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding Description}" VerticalAlignment="Center" HorizontalAlignment="Left" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Path}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Grid Grid.Row="1"
                  Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock
                    x:Name="ItemPermissionsLabel"
                    x:Uid="ItemPermissionsLabel"
                    Visibility="Visible"
                    Grid.Row="0"
                    Grid.Column="0"
                    Style="{StaticResource PropertyName}"
                    FontWeight="Bold"
                    HorizontalAlignment="Left" 
                    Text="Permissions" />

                <TextBlock
                    x:Name="ItemAllowLabel"
                    x:Uid="ItemAllowLabel"
                    Visibility="Visible"
                    Grid.Row="0"
                    Grid.Column="1"
                    HorizontalAlignment="Center" 
                    Style="{StaticResource PropertyName}"
                    Text="Allow" />

                <TextBlock
                    x:Name="ItemDenyLabel"
                    x:Uid="ItemDenyLabel"
                    Visibility="Visible"
                    Grid.Row="0"
                    Grid.Column="2"
                    HorizontalAlignment="Center" 
                    Style="{StaticResource PropertyName}"
                    Text="Deny" />
            </Grid>

            <ListView x:Name="lstPermissions" 
                      Grid.Row="2"
                      Grid.Column="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Id}" Visibility="Collapsed"/>
                                <TextBlock Text="{Binding Descripton}" VerticalAlignment="Center" HorizontalAlignment="Left" Width="170"/>
                                <CheckBox IsChecked="{Binding Allow}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="30"/>
                                <CheckBox IsChecked="{Binding Deny}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="30"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Button Name="AdvancedPermissions"
                    Grid.Row="3"
                    Grid.Column="0"
                    Content="Advanced permissions"/>

    </Grid>

    </StackPanel>
</local:PropertiesTab>