<Page
    x:Class="Files.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Files.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Files.Views"
    xmlns:local1="using:Files"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:usercontrols="using:Files.UserControls.MultitaskingControl"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    KeyboardAcceleratorPlacementMode="Hidden"
    NavigationCacheMode="Required"
    mc:Ignorable="d">
    <Page.Resources>
        <converters:WidthToRightMargin x:Key="WidthToRightMarginConverter" />
    </Page.Resources>
    <Page.KeyboardAccelerators>
        <KeyboardAccelerator
            Key="Number1"
            Invoked="NavigateToNumberedTabKeyboardAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="Number2"
            Invoked="NavigateToNumberedTabKeyboardAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="Number3"
            Invoked="NavigateToNumberedTabKeyboardAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="Number4"
            Invoked="NavigateToNumberedTabKeyboardAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="Number5"
            Invoked="NavigateToNumberedTabKeyboardAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="Number6"
            Invoked="NavigateToNumberedTabKeyboardAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="Number7"
            Invoked="NavigateToNumberedTabKeyboardAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="Number8"
            Invoked="NavigateToNumberedTabKeyboardAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="Number9"
            Invoked="NavigateToNumberedTabKeyboardAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="N"
            Invoked="OpenNewWindowAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="F4"
            Invoked="CloseSelectedTabKeyboardAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="W"
            Invoked="CloseSelectedTabKeyboardAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="T"
            Invoked="AddNewInstanceAccelerator_Invoked"
            Modifiers="Control" />
        <KeyboardAccelerator
            Key="T"
            Invoked="AddNewInstanceAccelerator_Invoked"
            Modifiers="Control,Shift" />
    </Page.KeyboardAccelerators>

    <Grid TabFocusNavigation="Cycle">
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                x:Name="SidebarColumn"
                Width="{x:Bind SidebarAdaptiveViewModel.SidebarWidth, Mode=OneWay}" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid
            x:Name="DragArea"
            Margin="38,0,0,0"
            Grid.ColumnSpan="2"
            Canvas.ZIndex="3"
            Height="40"
            VerticalAlignment="Top"
            HorizontalAlignment="Stretch"
            Background="Transparent"
            Loaded="DragArea_Loaded" />
        <Grid Padding="2,0,0,0" Margin="{x:Bind DragRegionWidth, Mode=OneWay, Converter={StaticResource WidthToRightMarginConverter}}"
            Canvas.ZIndex="4" VerticalAlignment="Top" Grid.Column="1">
            <usercontrols:HorizontalMultitaskingControl
                    x:Name="horizontalMultitaskingControl"
                    x:FieldModifier="public"
                    Height="50"
                    x:Load="{x:Bind converters:MultiBooleanConverter.OrConvert(AppSettings.IsHorizontalTabStripOn, local1:App.InteractionViewModel.IsHorizontalTabStripVisible), Mode=OneWay}"
                    Loaded="HorizontalMultitaskingControl_Loaded" />
        </Grid>
        <ContentPresenter Grid.ColumnSpan="2" Content="{x:Bind SelectedTabItem.Control, Mode=OneWay}" />
    </Grid>
</Page>